
def command(name, deps, line):
  AlwaysBuild(Alias(name, deps, line))

command("01", [], "./build_01_copy")
command("02", [], "./build_02_decompress")
command("03", [], "./build_03_collate")
command("04", [], "./build_04_upgrade")
